Before using:
=============

make sure following packages are installed
sudo pip install code-sniffer
sudo apt-get install graphviz

Also:
=====
Change the absolute path of the students folder in the *.conf file (!!!)
